# Часть 1
## Задание 1
```
#!/usr/bin/env python3
from flask import Flask

from mod_api import mod_api

app = Flask('vuln_app')
app.config['SECRET_KEY'] = 'F0cUzh8BgYJSLXAU8qDmClM0dE8GJTpsiyVEl3BCqQMCABp1U$f%'

app.register_blueprint(mod_api, url_prefix='/api')
```
**Недостаток:** секретный ключ (`SECRET_KEY`) прописан в коде приложения. Это создает проблему слива секретного ключа. Рекомендация: хранить ключи и секретные токены в переменных окружения. Пример исправления
```
import os
#!/usr/bin/env python3
from flask import Flask

from mod_api import mod_api

app = Flask('vuln_app')
app.config['SECRET_KEY'] = os.environ['SECRET_KEY']

app.register_blueprint(mod_api, url_prefix='/api')
```

## Задание 2
```
from flask_wtf import Form
from wtforms import TextField

class LoginForm(Form):
    username = TextField('username') 
    password = TextField('password')
```
**Недостаток:** поле пароля объявлено как `TextField`, а не как `PasswordField`. Это приводит к тому, что оно рендерится как `<input type="text">`, и вводимый пароль отображается в открытом виде на экране пользователя. Рекомендация: заменить `TextField` на `PasswordField`

## Задание3
```
def post(self):
    username = self.get_argument('username')
    password = self.get_argument('password').encode('utf-8')
    email = self.get_argument('mail')
try:
    username = username.lower()
    email = email.strip().lower()
    user = User({'username': username, 'password': password, 'email': email, 'date_joined': curtime()})
    user.validate()
    save_user(self.db_conn, user)
except Exception, e:
    return self.render_template("success_create.html")
```
Недостаток: пароль сохраняется в открытом виде в бд. Рекомендация: перед сохранением в бд делать хэширование с помощью различных алгоритмов
Также при ошибке регистрации происходит рендер страницы успешного создания, что неправильно (тут могу ошибаться, весь контекст не знаю)
Недостаток: отсутствует валидации входных данных (как минимум почта). Рекомендация: сделать функцию валидации почты/использовать тип EmailStr у pydantic.



# Часть 2
### Вопрос 1: Какие наиболее распространенные атаки предотвращает django?
1) Защита от атак на основе инъекций (- Django ORM автоматически экранирует пользовательский ввод и предотвращает SQL-инъекции.
2) Защита от XSS атак. Шаблонизатор Django **автоматически экранирует** специальные HTML-символы (`<`, `>`, `&`, `'`, `"`) при выводе переменных.
3) Cross-Site Request Forgery (CSRF). При загрузке страницы с формой Django генерирует уникальный секретный токен и вставляет его в скрытое поле формы через тег `{% csrf_token %}`. Когда форма отправляется, middleware `CsrfViewMiddleware` сверяет токен из тела запроса с токеном, который хранится в cookie.
4) Защита от кликджекинга - Django по умолчанию передает заголовок X-Frame-Options: DENY на все запросы. Это нужно для того, чтобы сайт нельзя было встроить в iframe. Это защищает от атаки, при которой злоумышленник встраивает целевой сайт в невидимый iframe на своей странице и накладывает поверх элементы-приманки.
5) SSL/HTTPS - помогает встраивать HTTPS в сервис
а) `SECURE_SSL_REDIRECT = True` - автоматический редирект с HTTP на HTTPS.
б) `SECURE_HSTS_SECONDS`- заголовок, который сообщает браузеру, что сайт работает только по HTTPS.
в) `SESSION_COOKIE_SECURE = True` - cookie сессии передаются только по HTTPS.
г) `CSRF_COOKIE_SECURE = True` - CSRF-cookie только по HTTPS.

Источники:
1) https://django.fun/docs/django/5.2/topics/security/
2) https://habr.com/ru/companies/vk/articles/823740/

### Вопрос 2: Описать, как устроено управление пользовательскими сессиями в django
HTTP - **stateless-протокол**: каждый запрос независим, сервер не помнит предыдущих взаимодействий. Сессии решают эту проблему. Сессии позволяют сохранять состояние между запросами.

При первом обращении Django генерирует уникальный session ID и отправляет его браузеру, сохраняем в cookie. Сами данные сессии (кто пользователь, его настройки, содержимое корзины) хранятся на сервере, по умолчанию - в таблице django_session

Если обновляете какие-либо данные при помощи ключа сессии тогда не надо беспокоиться о процессе сохранении
```
# Данное присваивание распознается как обновление сессии
# и данные будут сохранены
request.session['my_car'] = 'mini'
```
Если обновите информацию _внутри_ данных сессии, тогда Django не распознает эти изменения и не выполнит сохранение данных. В таких случаях вам надо явно указывать, что сессия была изменена.
```
# Объект сессии модифицируется неявно.
# Изменения НЕ БУДУТ сохранены!
request.session['my_car']['wheels'] = 'alloy'

# Явное указание, что данные изменены.
# Сессия будет сохранена, куки обновлены (если необходимо).
request.session.modified = True
```
Помимо БД, данные сессий можно хранить в кэше (Redis/Memcached), файлах или в подписанных cookie. Также Django предоставляет несколько настроек для управления временем жизни сессий:
1) SESSION_COOKIE_AGE - время жизни cookie сессии в секундах (по умолчанию 1209600)
2) SESSION_EXPIRE_AT_BROWSER_CLOSE - если True, то сессия истекает при закрытии браузера (по умолчание False)
3) SESSION_SAVE_EVERY_REQUEST - если True, то сессия пересохраняется при каждом запросе (по умолчание False)
4) SESSION_COOKIE_SECURE - если `True`, браузер будет отправлять cookie сессии только по HTTPS.
5) SESSION_COOKIE_DOMAIN - определяет для какого домена действует cookie (по умолчанию None => cookie привязана только к текущему домену)
Стоит помнить, что истекшие сессии не удаляются автоматически из БД. Для очистки нужно периодически запускать ``
```bash
python manage.py clearsessions
```



