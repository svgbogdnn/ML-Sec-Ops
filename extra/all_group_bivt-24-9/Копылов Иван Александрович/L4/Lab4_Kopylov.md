# Лабораторная работа № 4

## Часть 1
### Фрагмент 1
```
#!/usr/bin/env python3
from flask import Flask

from mod_api import mod_api

app = Flask('vuln_app')
app.config['SECRET_KEY'] = 'F0cUzh8BgYJSLXAU8qDmClM0dE8GJTpsiyVEl3BCqQMCABp1U$f%'

app.register_blueprint(mod_api, url_prefix='/api')
```
**Недостаток** : секретный ключ хранится в коде. 
**Последствия** : Человек, получивший доступ к коду (а это, как правило, довольно внушительный круг людей) может воспользоваться этим ключом и получить доступ к API.
\* если это код в проекте с открытым исходным кодом, то воспользоваться ключом может буквально кто угодно
**Как устранить** : не хранить ключи в коде. Для этого использовать переменные окружения и файлы настроек, которые хранить локально и никому не передавать.
### Фрагмент 2
```
from flask_wtf import Form  
from wtforms import TextField

class LoginForm(Form):  
username = TextField('username')  
password = TextField('password')
```
**Недостаток** : пароль вводится стандартной формой + ввод не валидируется. 
**Последствия** : система не считает, что в этом поле хранится чувствительная информация, что повышает риск того, что эти данные попадут, допустим, в логи. Также символы пароля не будут скрываться в интерфейсе, соответственно, его можно будет банально подглядеть. Бонусом ко всему из-за отсутствия валидации в это поле можно попробовать протянуть инъекции или забрутфорсить пароль.
**Как устранить** : использовать безопасное поле для пароля, проверять содержимое полей ещё на этапе фронта. Ещё пароли надо хэшировать, но, возможно, лучше это делать на более поздней стадии. Впрочем, не уверен.
### Фрагмент 3
```
def post(self):
    username = self.get_argument('username')
    password = self.get_argument('password').encode('utf-8')
    email = self.get_argument('mail')

try:
    username = username.lower()
    email = email.strip().lower()
    user = User({'username': username, 'password': password, 'email': email, 'date_joined': curtime()})
    user.validate()
    save_user(self.db_conn, user)
except Exception, e:
    return self.render_template("success_create.html")
```
**Недостаток** : ошибка никак не логируется и не показывается пользователю.
**Последствия** : пользователь будет думать, что данные сохранились, хотя это не так. Потом надо будет долго разбираться, что произошло. Также из-за отсутствия логирования можно не заметить и не предотвратить попытку атаки.
**Как устранить** : если ошибка произошла, сообщить пользователю о ней и записать происходящее в лог.
## Часть 2
### Какие наиболее распространенные атаки предотвращает django?
1. **XSS** : django автоматически экранирует html.
2. **CSRF** : в каждом POST-запросе проверяется секретный ключ, недоступный злоумышленнику.
3. **SQL-инъекции** : используются параметризированные запросы, и параметры экранируются.
4. **Clickjacking** : django по дефолту запрещает встраивать сайт в фрейм.
\* *и только тут я понял, что вопрос подразумевает просто список.*
### Как устроено управление пользовательскими сессиями в django
- При создании сессии клиенту отправляется ключ, который сохраняется в куку. При этом важно отметить, что вся остальная информация остаётся на сервере.
- При каждом запросе клиент отправляет ключ, сервер проверяет его и отдаёт данные клиенту.
- Сессия удаляется автоматически при истечении времени жизни токена или вручную, например, при выходе из аккаунта.
\* ключ псевдослучайный, его фактически невозможно подобрать, плюс, он дополнительно защищается флагами на уровне системы. 