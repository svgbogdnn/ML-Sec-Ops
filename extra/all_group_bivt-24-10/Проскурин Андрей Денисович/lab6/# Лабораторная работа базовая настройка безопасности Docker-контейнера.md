
## Цель работы  
  
Закрепить на практике базовые навыки обеспечения безопасности Docker-контейнеров:  
  
- проверить, от какого пользователя запускается контейнер по умолчанию;  
- настроить запуск контейнера от непривилегированного пользователя;  
- указать дополнительные меры безопасности для Docker-контейнера;  
- просканировать образ на уязвимости с помощью Trivy.  
  
---  
  
# Часть I. Проверка пользователя внутри стандартного контейнера  
  
## Используемый образ  
  
Для выполнения лабораторной работы был выбран базовый Docker-образ:  
  
alpine:3.20

Образ был скачан из Docker Hub командой:

docker pull alpine:3.20

После скачивания был проверен список локальных образов:

docker image ls

В списке локальных образов присутствовал образ:

REPOSITORY   TAG    IMAGE ID       CREATED      SIZE  
alpine       3.20   d9e853e87e55   8 days ago   13.6MB

## Проверка пользователя на хостовой системе

До запуска контейнера была выполнена команда:

whoami

Результат на хостовой системе:

andrey

Это означает, что команды в терминале macOS выполнялись от пользователя `andrey`. (я)

## Запуск контейнера

Контейнер был запущен командой:

docker run -it alpine:3.20 sh

После входа внутрь контейнера была выполнена команда:

whoami

Результат внутри контейнера:

root

## Вывод по Части I

В первой части был скачан образ `alpine:3.20` и проверено его наличие на локальной машине с помощью команды `docker image ls`.

До запуска контейнера команда `whoami` на хостовой системе показала имя пользователя:

После запуска контейнера и входа в командную оболочку `sh` команда `whoami` внутри контейнера показала:

root

Это означает, что стандартный контейнер на основе образа `alpine:3.20` по умолчанию запускается от пользователя `root`.

Запуск контейнера от `root` является потенциальным риском безопасности, так как при компрометации приложения злоумышленник получает максимальные права внутри контейнера. Поэтому в следующей части контейнер будет доработан так, чтобы запуск происходил от непривилегированного пользователя.

## Скриншот к Части I
![[Pasted image 20260425230349.png]]

---

# Часть II. Настройка запуска контейнера не от root-пользователя

## Dockerfile

Для второй части был создан Dockerfile на основе базового образа `alpine:3.20`.
```
FROM alpine:3.20  
  
# Создаем отдельную группу и пользователя без root-прав  
RUN addgroup -S appgroup && adduser -S appuser -G appgroup  
  
# Рабочая директория приложения  
WORKDIR /app  
  
# Меняем владельца рабочей директории  
RUN chown -R appuser:appgroup /app  
  
# Переключаемся на непривилегированного пользователя  
USER appuser  
  
# Команда по умолчанию  
CMD ["sh"]  
  
# Дополнительные настройки безопасности, которые стоит использовать:  
#  
# 1. Не запускать контейнер с флагом --privileged,  
# так как он выдает контейнеру расширенные права на хосте.  
#  
# 2. Запускать контейнер с запретом повышения привилегий:  
# docker run --security-opt no-new-privileges ...  
#  
# 3. Убирать лишние Linux capabilities:  
# docker run --cap-drop=ALL ...  
#  
# 4. Использовать read-only filesystem, если приложению не нужна запись:  
# docker run --read-only ...  
#  
# 5. Ограничивать ресурсы контейнера:  
# docker run --memory=128m --cpus=0.5 ...  
#  
# 6. Не монтировать docker.sock внутрь контейнера,  
# так как это может дать доступ к Docker daemon на хосте.  
#  
# 7. Использовать минимальные и проверенные базовые образы,  
# например alpine или distroless.  
#  
# 8. Регулярно сканировать образ на уязвимости через Trivy/Clair.
```
## Сборка образа

Образ был собран командой:

docker build -t secure-alpine:lab .

Сборка завершилась успешно:

naming to docker.io/library/secure-alpine:lab

## Запуск контейнера

После сборки контейнер был запущен командой:

docker run -it secure-alpine:lab

Внутри контейнера была выполнена команда:

whoami

Результат:

appuser

## Объяснение настроек Dockerfile

В Dockerfile были выполнены следующие действия:

1. В качестве базового образа был выбран минимальный образ:

FROM alpine:3.20

2. Была создана отдельная группа `appgroup` и пользователь `appuser` без root-прав:

RUN addgroup -S appgroup && adduser -S appuser -G appgroup

3. Была создана рабочая директория приложения:

WORKDIR /app

4. Права на директорию `/app` были переданы пользователю `appuser` и группе `appgroup`:

RUN chown -R appuser:appgroup /app

5. Выполнение контейнера было переключено на непривилегированного пользователя:

USER appuser

6. В качестве команды по умолчанию была указана оболочка:

CMD ["sh"]

## Дополнительные меры безопасности

В комментариях Dockerfile были указаны дополнительные меры безопасности, которые можно применять при запуске контейнера.

### 1. Не использовать `--privileged`

Флаг `--privileged` выдает контейнеру расширенные права на хостовой системе. Использование этого флага увеличивает риск компрометации хоста при взломе контейнера.

Небезопасный пример:

docker run --privileged image_name

### 2. Запускать контейнер с запретом повышения привилегий

Флаг `--security-opt no-new-privileges` запрещает процессам внутри контейнера получать новые привилегии.

Пример:

docker run --security-opt no-new-privileges secure-alpine:lab

### 3. Убирать лишние Linux capabilities

Флаг `--cap-drop=ALL` удаляет все дополнительные Linux capabilities у контейнера.

Пример:

docker run --cap-drop=ALL secure-alpine:lab

### 4. Использовать read-only filesystem

Флаг `--read-only` делает файловую систему контейнера доступной только для чтения, если приложению не требуется запись.

Пример:

docker run --read-only secure-alpine:lab

### 5. Ограничивать ресурсы контейнера

Ограничение памяти и CPU снижает риск злоупотребления ресурсами хостовой системы.

Пример:

docker run --memory=128m --cpus=0.5 secure-alpine:lab

### 6. Не монтировать `docker.sock` внутрь контейнера

Монтирование Docker socket внутрь контейнера может дать процессам внутри контейнера доступ к Docker daemon на хосте.

Небезопасный пример:

docker run -v /var/run/docker.sock:/var/run/docker.sock image_name

### 7. Использовать минимальные базовые образы

Минимальные образы, такие как `alpine` или `distroless`, содержат меньше пакетов. Это уменьшает поверхность атаки.

### 8. Регулярно сканировать образы

Образы необходимо регулярно проверять с помощью сканеров безопасности, например Trivy или Clair.

## Пример более безопасного запуска контейнера

Контейнер можно запускать с дополнительными флагами безопасности:

docker run -it \  
  --security-opt no-new-privileges \  
  --cap-drop=ALL \  
  --read-only \  
  --memory=128m \  
  --cpus=0.5 \  
  secure-alpine:lab

## Вывод по Части II

Во второй части был написан Dockerfile на основе образа `alpine:3.20`.

Внутри Dockerfile был создан непривилегированный пользователь `appuser`, после чего контейнер был настроен на запуск от этого пользователя с помощью инструкции:

USER appuser

После сборки и запуска контейнера команда `whoami` внутри контейнера показала:

appuser

Это подтверждает, что контейнер запускается не от `root`, а от непривилегированного пользователя.

Также в Dockerfile были указаны дополнительные меры безопасности: отказ от запуска с `--privileged`, использование `--security-opt no-new-privileges`, удаление лишних capabilities через `--cap-drop=ALL`, запуск с read-only filesystem, ограничение ресурсов контейнера и запрет на монтирование `docker.sock`.

## Скриншот к Части II
![[Pasted image 20260425231148.png]]

# Часть III. Сканирование образа с помощью Trivy

## Проверка версии Trivy

Перед сканированием была проверена версия установленного Trivy:

trivy --version

Результат:

Version: 0.70.0

## Сканирование образа

Для проверки безопасности был просканирован образ:

secure-alpine:lab

Сканирование выполнялось командой:

trivy image secure-alpine:lab

При первом запуске Trivy скачал базу уязвимостей:

Need to update DB  
Downloading vulnerability DB...  
Artifact successfully downloaded

После этого Trivy определил операционную систему внутри образа:

Detected OS family="alpine" version="3.20.10"

Также Trivy определил количество пакетов:

pkg_num=14

## Результат сканирования

Итоговый результат сканирования:

Report Summary  
  
![[Pasted image 20260425231252.png]]

Trivy не обнаружил известных уязвимостей:

Vulnerabilities: 0

## Проверка только HIGH и CRITICAL уязвимостей

Дополнительно был выполнен отдельный запуск Trivy только для уязвимостей уровней `HIGH` и `CRITICAL`:

trivy image --severity HIGH,CRITICAL secure-alpine:lab

Результат:

Report Summary  
![[Pasted image 20260425231302.png]]

Уязвимости высокого и критического уровня также не были обнаружены:

HIGH: 0  
CRITICAL: 0


## Вывод по Части III

В третьей части образ `secure-alpine:lab` был просканирован с помощью Trivy.

Сканирование показало:

Vulnerabilities: 0

Также отдельная проверка только по уязвимостям уровней `HIGH` и `CRITICAL` показала отсутствие уязвимостей:

HIGH: 0  
CRITICAL: 0

Это означает, что в образе не были обнаружены известные уязвимости по базе Trivy.

При этом Trivy предупредил, что версия `alpine 3.20.10` больше не поддерживается дистрибутивом. Поэтому для реального production-использования рекомендуется заменить базовый образ на более свежую поддерживаемую версию.

В рамках лабораторной работы образ можно считать безопасным на базовом уровне, так как:

- контейнер запускается не от пользователя `root`;
- внутри контейнера используется непривилегированный пользователь `appuser`;
- Trivy не обнаружил известных уязвимостей;
- уязвимости уровней `HIGH` и `CRITICAL` отсутствуют;
- в Dockerfile указаны дополнительные рекомендации по безопасному запуску контейнера.

## Скриншот к Части III
![[Pasted image 20260425231411.png]]

# Итоговый вывод

В ходе лабораторной работы была выполнена базовая настройка безопасности Docker-контейнера.

В первой части было показано, что стандартный контейнер на основе образа `alpine:3.20` по умолчанию запускается от пользователя `root`. Это является потенциальным риском безопасности, так как процесс внутри контейнера получает максимальные права в пределах контейнера.

Во второй части был создан собственный Dockerfile, в котором был добавлен непривилегированный пользователь `appuser`. После сборки и запуска нового образа команда `whoami` внутри контейнера показала `appuser`, что подтверждает успешный запуск контейнера не от `root`.

Также в Dockerfile были указаны дополнительные меры безопасности:

- не использовать `--privileged`;
- запускать контейнер с `--security-opt no-new-privileges`;
- удалять лишние capabilities через `--cap-drop=ALL`;
- использовать `--read-only`, если приложению не нужна запись в файловую систему;
- ограничивать ресурсы контейнера через `--memory` и `--cpus`;
- не монтировать `docker.sock` внутрь контейнера;
- использовать минимальные базовые образы;
- регулярно сканировать образы на уязвимости.

В третьей части образ `secure-alpine:lab` был просканирован с помощью Trivy. По результатам сканирования известных уязвимостей обнаружено не было:

Vulnerabilities: 0

Дополнительная проверка только по уязвимостям уровней `HIGH` и `CRITICAL` также показала отсутствие найденных уязвимостей.

При этом Trivy вывел предупреждение, что версия `alpine 3.20.10` больше не поддерживается дистрибутивом. Поэтому для production-среды рекомендуется использовать более свежую поддерживаемую версию базового образа.

Таким образом, цель лабораторной работы была достигнута: был проверен стандартный запуск контейнера, настроен запуск от непривилегированного пользователя, указаны дополнительные меры безопасности и выполнено сканирование образа на уязвимости.