# Лабораторная работа 6
## Изучение существующих CI/CD с точки зрения доступных инструментов ИБ

---  
  
### 1. Используемый инструмент  
  
Для проверки репозитория был выбран инструмент `truffleHog`.  
  
`truffleHog` — это инструмент для поиска секретов в git-репозиториях. Он анализирует историю изменений, коммиты, ветки и diff-файлы. Инструмент может находить:  
  
- пароли;  
- токены доступа;  
- приватные ключи;  
- секреты приложений;  
- строки с высокой энтропией;  
- другие чувствительные данные.  
  
Особенность инструмента заключается в том, что он проверяет не только актуальное состояние файлов, но и историю git. Это важно, потому что даже если секрет был удален из текущей версии файла, он мог остаться в старых коммитах.  
  
---  
  
### 2. Подготовка окружения  
  
Для выполнения лабораторной работы использовался Docker-контейнер с образом Python.  
  
Сначала был выполнен переход в рабочую папку:  
  
```bash  
cd "/mnt/c/Users/vkatr/OneDrive/Рабочий стол/Инфобез/lab6"
```

Далее был запущен Docker-контейнер:
```bash  
docker run -it -p 8080:8080 python:buster /bin/bash
```

После запуска контейнера была выполнена установка `truffleHog`:
```bash  
pip install truffleHog
```
___
### 3. Проверяемый репозиторий

Для анализа использовался репозиторий:
```bash  
https://github.com/OtusTeam/DevSecOps_secret-finding.git
```
Данный репозиторий был указан в методических материалах как специально подготовленный репозиторий для поиска секретов.
___
### 4. Запуск сканирования
Сканирование репозитория было выполнено командой:

```bash  
trufflehog https://github.com/OtusTeam/DevSecOps_secret-finding.git | tee -a log.txt
```
Команда `tee -a log.txt` использовалась для того, чтобы одновременно вывести результат проверки в терминал и сохранить его в файл `log.txt`.

После выполнения проверки лог был просмотрен командой:

```bash  
cat log.txt | more
```
Так как вывод оказался очень большим, дополнительно были использованы команды фильтрации по ключевым словам:
```bash  
grep -n -i -B6 -A8 "GITHUB_TOKEN:" log.txt > useful.txt
grep -n -i -B6 -A8 "PASSWORD=" log.txt >> useful.txt
grep -n -i -B6 -A8 "APP_SECRET" log.txt >> useful.txt
grep -n -i -B6 -A15 "BEGIN PRIVATE KEY" log.txt >> useful.txt
grep -n -i -B6 -A15 "BEGIN RSA PRIVATE KEY" log.txt >> useful.txt
```
Затем был просмотрен сокращенный файл с полезными находками:
```bash  
cat useful.txt | more
```
___
### 5. Найденные секреты

| № | Расположение секрета | Найденное значение | Почему это является секретом | Правильный способ хранения | Рекомендации по устранению |
|---|---|---|---|---|---|
| 1 | `.github/workflows/release.yml`, строка около 72 | `GITHUB_TOKEN: cee3257f0e59d1d4975a35429cffc3d8b5fabe15ca2e9d47243cbc446d4894a5` | Это токен GitHub, записанный прямо в CI/CD workflow. Такой токен может использоваться для доступа к GitHub API, репозиторию, публикации релизов или выполнения действий от имени владельца токена. Если токен активен, злоумышленник может получить несанкционированный доступ к репозиторию или процессу CI/CD. | Хранить токен в GitHub Actions Secrets и использовать обращение вида `${{ secrets.GITHUB_TOKEN }}` или `${{ secrets.RELEASE_TOKEN }}`. | Удалить токен из файла workflow. Отозвать найденный токен в настройках GitHub. Выпустить новый токен с минимально необходимыми правами. Добавить новый токен в GitHub Actions Secrets. В файле `.github/workflows/release.yml` заменить прямое значение на `${{ secrets.GITHUB_TOKEN }}` или `${{ secrets.RELEASE_TOKEN }}`. |
| 2 | `webgoat-server/privatekey.key`, строка 1 | `-----BEGIN PRIVATE KEY-----` | Это приватный криптографический ключ. Приватные ключи не должны храниться в репозитории, так как они могут использоваться для аутентификации, подписи, TLS или других криптографических операций. При утечке такого ключа злоумышленник может использовать его как доверенный ключ. | Хранить приватные ключи в защищенном хранилище секретов: HashiCorp Vault, Kubernetes Secrets, Docker Secrets или CI/CD Secrets. | Удалить файл `privatekey.key` из репозитория. Считать найденный ключ скомпрометированным. Перевыпустить новый приватный ключ и сертификат, если он использовался. Добавить `*.key`, `*.pem`, `*.p12`, `*.jks` в `.gitignore`. Удалить старый ключ из истории git с помощью BFG Repo-Cleaner или `git filter-repo`. |
| 3 | `docker/.env`, строка 2 | `APP_SECRET=666666666666666` | Это секрет приложения. Такие значения обычно используются для подписи cookies, JWT, сессий или внутренних токенов. Если злоумышленник получит этот секрет, он потенциально сможет подделывать служебные данные приложения. | Хранить значение в переменных окружения или secret manager. В репозитории оставить только файл `.env.example` с шаблонными значениями без реальных секретов. | Удалить реальное значение `APP_SECRET` из файла `.env`. Сгенерировать новый секрет приложения. Добавить `.env` в `.gitignore`. Создать `.env.example`, где вместо реального значения будет указано `APP_SECRET=change_me`. Реальное значение передавать через переменные окружения при запуске приложения или через secrets CI/CD. |
| 4 | `docker/.env`, строка 3 | `USER=alex675` | Сам по себе логин не всегда является секретом, но в данном случае он находится рядом с паролем и образует часть учетной пары. Это упрощает злоумышленнику использование найденных учетных данных. | Не хранить реальные учетные данные в `.env` внутри репозитория. Использовать переменные окружения CI/CD или secret-хранилище. В примере оставить только шаблонное значение. | Удалить реальный логин из `.env`. Заменить его на переменную окружения, например `USER=${APP_USER}`. В `.env.example` указать шаблон `USER=change_me`. Проверить, где используется данная учетная запись, и при необходимости изменить логин или ограничить права доступа. |
| 5 | `docker/.env`, строка 4 | `PASSWORD=#!qerge34t423sdfvdsfgeR` | Это пароль в открытом виде. Пароль может давать доступ к учетной записи, базе данных, сервису или приложению. Его хранение в git является критичной ошибкой, так как даже после удаления из файла пароль может остаться в истории коммитов. | Перенести пароль в secret-хранилище. Найденный пароль необходимо считать скомпрометированным, заменить на новый, а файл `.env` добавить в `.gitignore`. | Немедленно заменить найденный пароль. Удалить пароль из `.env`. Добавить `.env` в `.gitignore`. В `.env.example` оставить только `PASSWORD=change_me`. Проверить историю git и удалить старое значение из коммитов с помощью BFG Repo-Cleaner или `git filter-repo`. В дальнейшем хранить пароль только в GitHub Actions Secrets, GitLab CI/CD Variables, Vault, Docker Secrets или Kubernetes Secrets. |
