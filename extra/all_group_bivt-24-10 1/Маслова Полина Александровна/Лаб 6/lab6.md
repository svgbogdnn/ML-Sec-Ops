1. Первый секрет: private final static String WEBGOAT_URL = "aHR0cDovL2xvY2FsaG9zdC9XZWJHb2F0L2NhdGNoZXI/UFJPUEVSVFk9eWVz"; Написано, что это url, то есть ссылка, надо придумать, как бы её раскодировать. Расположние секрета: webgoat-5.4/src/main/java/org/owasp/webgoat/lessons/BlindScript.java
2. Начинаем раскодировать ссылку.   Декодеры в интернете мне выдали такое http://localhost/WebGoat/catcher?PROPERTY=yes. Это похоже на какой-то эндпоинт. Он используется в методе `executeSpyWare()` для отправки украденной истории браузера пользователя на удаленный сервер.
3. Это является секретом, так как есть кодировка, то есть попытка скрыть реальное содержимое и используется для вредоносной цели
4. Правильный способ хранения - не кодировать URL в Base64 и использовать переменные окружения для хранения секретов 
---
1. Второе место: var coupons = ["nvojubmq", "emph", "sfwmjt", "faopsc", "fopttfsq", "pxuttfsq"];
   Расположение секрета: webgoat-5.4/src/main/webapp/javascript/clientSideValidation.js
2. Эти элементы проходят через функцию decrypt. Эта функция раскодирует элементы шифром Цезаря. 
3. Это является секретом, так как информация хотя бы как-то закодирована и используется в клиентском коде 
4. Чтобы защитить подобные данные надо делать их валидацию на сервисе или хранить в БД с хешированием, но точно не хранить в коде 
---
1. Третье место: if ("B_Admin443".equals(me)) { s.setAdmin(true); }
   Расположение: webgoat-5.4/src/main/java/org/owasp/webgoat/lessons/BlindScript.java
2. Это секретный пароль, который даёт пользователю права администратора. Пароль это B_Admin443, который открыто хранится в коде 
3. Это является секретом, так как это пароль для получения повышенного доступа 
4. Правильный способ хранения: точно не хранить в коде, хранить в БД с хешированием и использовать многофакторную аутентификацию для администраторов 
---
1. Четвертый секрет: 
   Calendar cal2 = Calendar.getInstance();
	cal2.set(2010, 1, 13); 
	if (cal1.getTime().after(cal2.getTime())) {
    String query = "DELETE employee";
	}
	Расположение: webgoat-5.4/src/main/java/org/owasp/webgoat/lessons/BlindScript.java
2. Это код, который активируется после 13 января 2010 года и удаляет все записи из таблицы employee
3. Это является секретом, так как здесь скрыто вредоностное поведение и жёстко закодирована дата как триггер 
4. Правильный способ хранения: не писать скрытые триггеры, все изменения в коде должны быть явными и логируемыми, удаление должно требовать явного подтверждения 
---
1. И пятый секрет: String userHome = System.getProperty("user.home") + "\\Local Settings\\Temporary Internet Files";
   Расположение: webgoat-5.4/src/main/java/org/owasp/webgoat/lessons/BlindScript.java
2. Это является секретом, так как это путь к чувствительным данным пользователя (кэш браузера, временные файлы). Код собирает имена файлов из этой папки и отправляет их на удаленный сервер.
3. Правильный способ хранения: не хардкодить пути к файлам пользователя, использовать стандартные API для доступа к временным файлам, не собирать личные данные пользователя без явного согласия