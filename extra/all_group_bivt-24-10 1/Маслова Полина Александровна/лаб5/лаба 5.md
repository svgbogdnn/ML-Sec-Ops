### Часть 1
1. Смотрю образы, которые у меня уже есть
   ```
    docker image ls
                                                                                                    i Info →   U  In Use
IMAGE                                   ID             DISK USAGE   CONTENT SIZE   EXTRA
ket9/otus-devsecops-owasp-rest:latest   9da7b7d1e2a8        451MB          120MB    U
ket9/otus-devsecops:latest              a85cce3b4064        720MB          163MB    U
mysql:5.7                               4bc6bc963e6d        700MB          149MB    U
   ```
   2. Выполняю команду whoami
   3. Запускаю командную оболочку с помощью команды docker run -it ket9/otus-devsecops:latest sh и вижу #, что говорит о том, что я внутри образа
   4. Запускаю команду whoami снова, уже внутри образа и вижу root
Скриншот с проделанной работой: 
(фото 1)

---

### Часть 2
1. Для этого же образа собираю Dockerfile. Здесь я создаю системного пользователя myuser с помощью команды RUN и задаю ему id 500. И с помощью команды USER myuser переключаюсь на пользователя myuser, чтобы запустить образ от его имени. 
   ```
FROM ket9/otus-devsecops:latest

  

RUN adduser --system --uid 500 myuser

  

USER myuser

EXPOSE 8080
   ```
   2. Собираю образ и запускаю его в терминале, внутри образа даю команду whoami и получаю myuser, это значит, что всё получилось, ура
      (фото 2)
   3. Доп настройки и утилиты, с которыми можно более безопасно запускать файл: 
```
#Доп настройки

#- COPY --chown=myuser:myuser . /app

#копирование файлов с пользователем myuser

#

#- WORKDIR /papka

# указать рабочую директорию, где должно быть приложение, чтобы оно не ушло, куда не стоило бы

#

#Утилиты для запуска:

    # --user 500 (явно указать пользователя)

    # --read-only (только чтение, без записи)

    # --security-opt=no-new-privileges (запретить повышение привилегий)
```

---

### Часть 3
1. Прогоняю образ через сканер trivy. Запускаю его командой docker run --rm aquasec/trivy image --severity CRITICAL,HIGH ket9/otus-devsecops:latest > trivy-report.txt, чтобы вывод сохранился в текстовый файл. Так как в консоль весь он не помещается. 
2. В файле получаю длинный вывод, но ошибки везде одинаковые, просто они встречаются в разных библиотеках apache2. Все ошибки есть на скриншоте
(фото 3)

**Критические уязвимости:**
- **CVE-2017-3167** - обход аутентификации
- **CVE-2017-3169** - ошибка в mod_ssl 
- **CVE-2017-7679** - переполнение буфера в mod_mime
- **CVE-2017-9788** - чтение неинициализированной памяти
- **CVE-2018-1312** - слабая генерация Digest-аутентификации
**Высокий уровень уязвимости:**
- **CVE-2016-874** - обход аутентификации, доступ к запрещённым ресурсам
- **CVE-2017-15710** - отказ в обслуживании, DoS
- **CVE-2017-15715** - Выполнение PHP-кода
- **CVE-2017-7668** - DoS, утечка памяти
- **CVE-2017-9798** - Утечка данных из памяти
- **CVE-2018-17199** - Продление атаки
- **CVE-2019-0217** - Обход пароля