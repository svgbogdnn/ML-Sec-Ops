# Отчёт о проведении SAST-анализа мобильного приложения
 
**Лабораторная работа №7**

---

## 1. Введение

### 1.1 Цель работы
Развернуть инструмент SAST-анализа мобильных приложений (MobSF) и провести тестирование безопасности мобильного приложения.

### 1.2 Инструментарий
- **Инструмент:** Mobile Security Framework (MobSF) v3.x
- **Способ развёртывания:** Docker Container
- **Объект анализа:** TikTok.apk (com.zhiliaoapp.musically)
- **Версия приложения:** 32.7.4
- **Размер:** 67.3 МБ

### 1.3 Методология
SAST (Static Application Security Testing) — метод анализа безопасности приложений, который проверяет исходный код и бинарные файлы без выполнения программы. MobSF автоматически анализирует:
- AndroidManifest.xml
- Сетевую конфигурацию
- Разрешения приложения
- Исходный код на наличие уязвимостей
- Hardcoded secrets

---

## 2. Развёртывание MobSF

### 2.1 Установка через Docker

```powershell
# Создание директории для данных
mkdir -p ~/mobsf-data

# Запуск контейнера MobSF
docker run -it --rm \
  -p 8000:8000 \
  -v ~/mobsf-data:/home/mobsf/MobSF/userdata \
  opensecurity/mobile-security-framework-mobsf:v3.1.1
```

### 2.2 Проверка работоспособности

После запуска контейнера веб-интерфейс доступен по адресу:
- **URL:** http://localhost:8000
- **Статус:** Работает

---

## 3. Информация о тестируемом приложении

| Параметр | Значение |
|----------|----------|
| **Название** | TikTok |
| **Package Name** | com.zhiliaoapp.musically |
| **Версия** | 32.7.4 |
| **Размер файла** | 67.3 МБ |
| **Min SDK** | 23 (Android 6.0) |
| **Target SDK** | 33 (Android 13) |
| **Security Score** | 45/100 |
| **Risk Level** | HIGH |
| **Grade** | C |

**Обоснование выбора:** TikTok — популярное приложение с большим количеством функций (видео, стримы, чаты, платежи), что делает его интересным объектом для анализа безопасности.

---

## 4. Результаты сканирования

### 4.1 Сводная статистика уязвимостей

| Severity | Количество | Процент |
|----------|------------|---------|
| **High** | 9 | 15% |
| **Medium** | 18 | 30% |
| **Low/Info** | 33 | 55% |
| **ВСЕГО** | **60** | **100%** |

---

### 4.2 App Permissions — Опасные разрешения

MobSF обнаружил **6 разрешений** со статусом **dangerous**.

| Разрешение | Уровень риска | Описание угрозы |
|------------|--------------|-----------------|
| `ACCESS_FINE_LOCATION` |  **High** | Точное GPS-отслеживание в реальном времени. Риск утечки геоданных и слежки за пользователем |
| `ACCESS_COARSE_LOCATION` |  **Medium** | Приблизительное местоположение через сеть. Может использоваться для профилирования |
| `ACCESS_MEDIA_LOCATION` |  **Medium** | Извлечение геолокации из EXIF-данных фотографий и видео. Утечка метаданных |
| `CAMERA` |  **Medium** | Доступ к камере. Риск несанкционированной съёмки |
| `RECORD_AUDIO` |  **Medium** | Запись звука с микрофона. Перехват приватных разговоров |
| `READ_CONTACTS` |  **High** | Полный доступ к контактам. Утечка личных данных третьих лиц |

**Анализ:**
- Для видеоприложения разрешения `CAMERA` и `RECORD_AUDIO` **обоснованы**
- `ACCESS_FINE_LOCATION` **избыточно** — достаточно приблизительной геолокации для рекомендаций контента
- `READ_CONTACTS` **критично** — не очевидно зачем приложению доступ к контактам

**Рекомендации:**
1. Запрашивать разрешения **в рантайме**, а не при установке
2. Объяснять пользователю **цель** каждого запроса
3. Предоставлять **fallback** для пользователей, отказавших в разрешениях
4. Удалить `READ_CONTACTS` если функционал не требует доступа к адресной книге

---

### 4.3 Network Security — Уязвимости сетевой безопасности

Обнаружено **4 проблемы** уровня **HIGH** и выше.

#### HIGH-1: Cleartext HTTP разрешён глобально

**Severity:** HIGH  
**CWE:** CWE-319 (Cleartext Transmission of Sensitive Information)

**Описание:**
В `AndroidManifest.xml` установлен флаг:
```xml
<application
    android:usesCleartextTraffic="true"
    ... >
```

Это разрешает передачу данных по **незашифрованному HTTP** для всех доменов.

**Риск:**
- Перехват трафика злоумышленником (MITM-атака)
- Утечка чувствительных данных (токены, персональная информация)
- Подмена контента и инъекция вредоносного кода

**Митигация:**
```xml
<application
    android:usesCleartextTraffic="false"
    android:networkSecurityConfig="@xml/network_security_config"
    ... >
```

Создать `res/xml/network_security_config.xml`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
```

---

#### HIGH-2: Обход Certificate Pinning

**Severity:** HIGH  
**CWE:** CWE-295 (Improper Certificate Validation)

**Описание:**
В конфигурации сети обнаружен механизм **обхода проверки сертификатов** (certificate pinning bypass).

**Риск:**
- Возможность подделки SSL-сертификата
- Перехват зашифрованного трафика через прокси (Charles, mitmproxy)
- Компрометация защищённых каналов связи

**Митигация:**
- Удалить код, позволяющий обходить certificate pinning
- Внедрить строгий **Public Key Pinning** для критичных API-доменов
- Использовать библиотеки с автоматической проверкой сертификатов (OkHttp, Retrofit)

---

#### HIGH-3: Cleartext для zero-rating.tiktok.com

**Severity:** HIGH  
**Описание:**
В domain-specific конфигурации разрешён HTTP-трафик для домена `zero-rating.tiktok.com`.

**Риск:**
- Специфичный домен для zero-rating (бесплатный трафик) передаёт данные без шифрования
- Утечка метаданных о пользователях

**Митигация:**
- Настроить HTTPS для **всех доменов без исключений**
- Использовать HSTS (HTTP Strict Transport Security)

---

#### WARNING: Доверие системным сертификатам

**Severity:** WARNING  
**Описание:**
Приложение доверяет всем системным CA-сертификатам по умолчанию.

**Рекомендация:**
- Ограничить список доверенных CA только необходимыми
- Использовать custom CA для внутренних сервисов

---

### 4.4 Manifest Analysis — Анализ AndroidManifest.xml

Обнаружено **2 проблемы** уровня **HIGH** и **37 предупреждений**.

#### HIGH-1: Устаревший minSdkVersion

**Severity:** HIGH  
**Проблема:** `minSdkVersion=23` (Android 6.0 Marshmallow)

**Риск:**
- Android 6.0 **не получает обновления безопасности** с 2019 года
- Известные уязвимости платформы остаются неисправленными
- Отсутствие современных механизмов защиты (Scoped Storage, Privacy Features, Runtime Permissions)

**Митигация:**
```gradle
android {
    defaultConfig {
        minSdkVersion 29  // Android 10
        targetSdkVersion 33
    }
}
```

**Обоснование:**
- Android 10+ получают регулярные security patches
- Современные механизмы защиты приватности
- Улучшенная работа с разрешениями

---

#### HIGH-2: Глобальный cleartext traffic

**Severity:** HIGH  
**Проблема:** `android:usesCleartextTraffic="true"` в `<application>`

**Риск:** (см. Network Security HIGH-1)

**Митигация:** (см. выше)

---

#### WARNING: Экспортированные компоненты

**Количество:** 26 Activity, 3 Service, 5 BroadcastReceiver

**Проблема:**
Компоненты объявлены с `android:exported="true"` без защиты `android:permission`.

**Риск:**
- Любое приложение может вызвать Activity/Service
- Утечка данных через Intent
- Запуск компонентов злоумышленником

**Митигация:**
```xml
<activity
    android:name=".MainActivity"
    android:exported="false" />

<!-- Или с защитой -->
<activity
    android:name=".SensitiveActivity"
    android:exported="true"
    android:permission="com.tiktok.permission.ACCESS_ACTIVITY" />
```

---

### 4.5 Code Analysis — Уязвимости в коде

Обнаружено **3 проблемы** уровня **HIGH** и **10 предупреждений**.

#### HIGH-1: ECB-режим шифрования

**Severity:** HIGH  
**CWE:** CWE-327 (Use of a Broken or Risky Cryptographic Algorithm)  
**Файл:** `X/C1531540pxZ.java`

**Описание:**
Используется режим **ECB** (Electronic Codebook) для шифрования данных. Одинаковые блоки открытого текста дают одинаковые блоки шифротекста.

**Риск:**
- Паттерны в данных остаются видимыми
- Возможность криптоанализа
- Не соответствует современным стандартам безопасности (NIST)

**Митигация:**
```java
// НЕБЕЗОПАСНО:
Cipher.getInstance("AES/ECB/PKCS5Padding")

// БЕЗОПАСНО:
Cipher.getInstance("AES/GCM/NoPadding")  // с уникальным IV
// ИЛИ
Cipher.getInstance("AES/CBC/PKCS5Padding")  // с случайным IV
```

---

#### HIGH-2: Слабый алгоритм шифрования

**Severity:** HIGH  
**CWE:** CWE-327  
**Файл:** `com/heytap/mcssdk/utils/c.java`

**Описание:**
Используется устаревший или слабый криптографический алгоритм (возможно DES, 3DES или RC4).

**Риск:**
- Алгоритм может быть взломан brute-force
- Не соответствует современным стандартам (NIST, FIPS)
- Уязвимость к криптоанализу

**Митигация:**
- Заменить на **AES-256-GCM** или **ChaCha20-Poly1305**
- Использовать ключи длиной минимум **256 бит**
- Реализовать правильное управление ключами (Android KeyStore)

---

#### HIGH-3: World Writable SharedPreference

**Severity:** HIGH  
**CWE:** CWE-732 (Incorrect Permission Assignment for Critical Resource)  
**Файл:** `SettingsFragmentDelegate.java`, `yd/E.java`

**Описание:**
Файл или SharedPreference создан с флагом **MODE_WORLD_WRABLE**. Любое приложение может записать данные в этот файл.

**Риск:**
- Любое приложение с разрешением на чтение внешнего хранилища может получить доступ к данным
- Возможность инъекции вредоносных данных
- Утечка токенов и настроек

**Митигация:**
```java
// НЕБЕЗОПАСНО:
getSharedPreferences("prefs", Context.MODE_WORLD_WRABLE);

// БЕЗОПАСНО:
getSharedPreferences("prefs", Context.MODE_PRIVATE);
```

Если данные нужно расшарить с другим приложением — использовать **Content Provider** с правами доступа.

---

#### WARNING: Hardcoded Secrets

**Severity:** WARNING  
**CWE:** CWE-798 (Use of Hard-coded Credentials)

**Найдено:**
```
"google_api_key":"AIzaSyCKvlAvtCaEuj0WuF1gxesJv_j6hHuoEHo"
"facebook_client_token":"c5a29925ae6d41fe68136a6edfc5749f"
"firebase_database_url":"https://td-project-01.firebaseio.com"
```

**Риск:**
- API-ключи извлечены из APK
- Злоумышленник может использовать ключи
- Квоты и лимиты могут быть исчерпаны

**Митигация:**
- Вынести ключи в **переменные окружения** или **BuildConfig**
- Использовать **backend-прокси** для доступа к внешним API
- Регулярно **ротировать** ключи

---

#### WARNING: Небезопасный генератор случайных чисел

**Severity:** WARNING  
**CWE:** CWE-330 (Use of Insufficiently Random Values)

**Описание:**
Используется `java.util.Random` вместо `java.security.SecureRandom`.

**Риск:**
- Предсказуемые значения
- Компрометация токенов, ключей, nonce

**Митигация:**
```java
// НЕБЕЗОПАСНО:
Random random = new Random();

// БЕЗОПАСНО:
SecureRandom random = new SecureRandom();
```

---

#### WARNING: Слабые хеш-функции

**Severity:** WARNING  
**CWE:** CWE-328

**Найдено:**
- Использование **MD5** (известны коллизии)
- Использование **SHA-1** (известны коллизии)

**Митигация:**
- Заменить на **SHA-256** или **SHA-3**
- Для паролей использовать **bcrypt**, **scrypt** или **Argon2**

---

#### WARNING: Запись чувствительных данных в логи

**Severity:** WARNING  
**CWE:** CWE-532 (Insertion of Sensitive Information into Log File)

**Описание:**
В логах могут попадать чувствительные данные (токены, идентификаторы пользователей).

**Митигация:**
- Отключить логирование в production-сборках
- Использовать **ProGuard/R8** для удаления логов
- Применять фильтры для маскирования чувствительных данных

---

## 5. План митигации обнаруженных рисков

### 5.1 Приоритизация исправлений

| Приоритет | Уязвимости | Срок | Баллы CVSS (оценка) |
|-----------|------------|------|---------------------|
| **Critical** | World Writable, Hardcoded Secrets, ECB Mode | **1 неделя** | 8.5-9.5 |
| **High** | Cleartext HTTP, Certificate Pinning Bypass, Weak Crypto | **2 недели** | 7.0-8.4 |
| **Medium** | minSdkVersion=23, Dangerous Permissions, Exported Components | **1 месяц** | 4.0-6.9 |
| **Low** | Logging, Weak Hashes, Insecure Random | **2 месяца** | 0.1-3.9 |

---

### 5.2 Детальный план действий

#### Critical Priority (Срок: 1 неделя)

**1. Исправить World Writable SharedPreference**

**Проблема:** Файлы доступны для записи любому приложению

**Действия:**
1. Найти все места создания SharedPreferences
2. Заменить `MODE_WORLD_WRABLE` на `MODE_PRIVATE`
3. Если нужен обмен данными — использовать Content Provider
4. Протестировать функциональность

**Ответственный:** Android Developer  
**Статус:**  In Progress  
**Проверка:** Повторное сканирование MobSF

---

**2. Удалить Hardcoded Secrets**

**Проблема:** API-ключи захардкожены в коде

**Действия:**
1. Извлечь все API-ключи из кода
2. Перенести в `gradle.properties` или переменные окружения
3. Настроить backend-прокси для чувствительных операций
4. Отозвать скомпрометированные ключи
5. Сгенерировать новые

**Ответственный:** Backend Developer + DevOps  
**Статус:**  Planned  
**Проверка:** Поиск строк в коде через grep

---

**3. Заменить ECB-режим шифрования**

**Проблема:** Использование AES/ECB

**Действия:**
1. Провести аудит всех криптографических операций
2. Заменить ECB на **AES-256-GCM**
3. Внедрить Android KeyStore для хранения ключей
4. Обновить библиотеки шифрования

**Ответственный:** Security Engineer  
**Статус:** In Progress  
**Проверка:** Code review + статический анализ

---

#### High Priority (Срок: 2 недели)

**4. Отключить Cleartext HTTP**

**Проблема:** Разрешён незашифрованный трафик

**Действия:**
1. Установить `android:usesCleartextTraffic="false"`
2. Создать Network Security Config
3. Настроить HTTPS для всех доменов
4. Включить HSTS
5. Протестировать все сетевые вызовы

**Ответственный:** Android Developer  
**Статус:** Planned  
**Проверка:** Wireshark/tcpdump для проверки шифрования

---

**5. Устранить Certificate Pinning Bypass**

**Проблема:** Код позволяет обходить проверку сертификатов

**Действия:**
1. Найти и удалить код bypass
2. Внедрить строгий Public Key Pinning
3. Протестировать с самоподписанными сертификатами

**Ответственный:** Security Engineer  
**Статус:** Backlog  
**Проверка:** Попытка MITM-атаки через mitmproxy

---

**6. Усилить криптографию**

**Проблема:** Слабые алгоритмы

**Действия:**
1. Заменить MD5/SHA-1 на SHA-256
2. Заменить DES/3DES на AES-256
3. Обновить библиотеки (Bouncy Castle, Tink)

**Ответственный:** Android Developer  
**Статус:** Planned  
**Проверка:** MobSF Code Analysis

---

#### Medium Priority (Срок: 1 месяц)

**7. Поднять minSdkVersion**

**Проблема:** Поддержка Android 6.0 (не получает патчи)

**Действия:**
1. Поднять `minSdkVersion` до **29** (Android 10)
2. Протестировать на совместимость
3. Проанализировать статистику пользователей
4. Уведомить пользователей об окончании поддержки

**Ответственный:** Product Manager + QA  
**Статус:** Discussion  
**Проверка:** Google Play Console Analytics

---

**8. Ограничить экспорт компонентов**

**Проблема:** 26 Activity, 3 Service экспортированы без защиты

**Действия:**
1. Провести аудит всех компонентов
2. Установить `exported="false"` для внутренних
3. Добавить `android:permission` для публичных
4. Протестировать межприложенное взаимодействие

**Ответственный:** Android Developer  
**Статус:** Planned  
**Проверка:** Drozer/Frida для проверки экспорта

---

**9. Минимизировать dangerous permissions**

**Проблема:** Избыточные разрешения

**Действия:**
1. Пересмотреть необходимость `READ_CONTACTS`
2. Удалить `ACCESS_BACKGROUND_LOCATION` если не нужно
3. Запрашивать разрешения в рантайме
4. Добавить объяснения в UI

**Ответственный:** UX Designer + Developer  
**Статус:** Planned  
**Проверка:** Проверка через `adb shell dumpsys package`

---

#### Low Priority (Срок: 2 месяца)

**10. Очистить логи**

**Проблема:** Чувствительные данные в логах

**Действия:**
1. Настроить ProGuard для удаления логов
2. Внедрить кастомный логгер с фильтрацией
3. Провести code review

**Ответственный:** DevOps  
**Статус:** Backlog

---

**11. Заменить небезопасный Random**

**Проблема:** Использование `java.util.Random`

**Действия:**
1. Найти все использования Random
2. Заменить на `SecureRandom`
3. Протестировать генерацию токенов

**Ответственный:** Android Developer  
**Статус:** Backlog

---

### 5.3 Контрольные точки

| Этап | Срок | Критерии приёмки |
|------|------|------------------|
| **Sprint 1** | Неделя 1 | Исправлены Critical уязвимости |
| **Sprint 2** | Неделя 2 | Исправлены High уязвимости |
| **Sprint 3** | Неделя 3-4 | Исправлены Medium уязвимости |
| **Retest** | Неделя 5 | Повторное сканирование MobSF |
| **CI/CD Integration** | Неделя 6-8 | Автоматическое SAST в пайплайне |

---

### 5.4 Метрики успеха

- **Security Score:** 45/100 → **85/100**
- **High Vulnerabilities:** 9 → **0**
- **Critical Vulnerabilities:** 3 → **0**
- **Grade:** C → **A**

---

## 6. Выводы

### 6.1 Общая оценка безопасности

В ходе SAST-анализа мобильного приложения **TikTok** было обнаружено:
-  **9 уязвимостей уровня HIGH**
-  **18 уязвимостей уровня MEDIUM**
-  **33 информационных замечаний**

**Security Score:** 45/100 (**HIGH RISK**)

### 6.2 Критические проблемы

**Наиболее опасные уязвимости:**
1. **World Writable SharedPreference** — любое приложение может получить доступ к данным
2. **Hardcoded API Keys** — ключи извлечены из APK
3. **ECB-режим шифрования** — уязвимость к криптоанализу
4. **Cleartext HTTP** — передача данных без шифрования
5. **Устаревший minSdkVersion** — поддержка непропатченных версий Android
